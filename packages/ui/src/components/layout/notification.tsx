import { useEffect } from "react";

export const AppNotification = ({ children }) => {
  // useEffect(() => {
  //   if (notice.type && notice.message) {
  //     toast[type](notice.message);
  //   }
  // }, []);
  // return (
  //   <>
  //     {children}
  //     <Toaster />
  //   </>
  // );
};

export default AppNotification;
