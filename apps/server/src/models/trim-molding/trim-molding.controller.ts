import {
  Controller,
  // Get,
  // Post,
  // Body,
  // Patch,
  // Param,
  // Delete,
} from "@nestjs/common";

// import { TrimMoldingService } from "./trim-molding.service";
// import { CreateTrimMoldingDto } from './dto/create-trim-molding.dto';
// import { UpdateTrimMoldingDto } from './dto/update-trim-molding.dto';

@Controller("trim-molding")
export class TrimMoldingController {
  // constructor(private readonly trimMoldingService: TrimMoldingService) {}
  // @Post()
  // create(@Body() createTrimMoldingDto: CreateTrimMoldingDto) {
  //   return this.trimMoldingService.create(createTrimMoldingDto);
  // }
  // @Get()
  // findAll() {
  //   return this.trimMoldingService.findAll();
  // }
  // @Get(':id')
  // findOne(@Param('id') id: string) {
  //   return this.trimMoldingService.findOne(+id);
  // }
  // @Patch(':id')
  // update(@Param('id') id: string, @Body() updateTrimMoldingDto: UpdateTrimMoldingDto) {
  //   return this.trimMoldingService.update(+id, updateTrimMoldingDto);
  // }
  // @Delete(':id')
  // remove(@Param('id') id: string) {
  //   return this.trimMoldingService.remove(+id);
  // }
}
